<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <!-- Github remote -->
  <remote  name="github"
           fetch="https://github.com/" />

  <!-- Gitlab remote -->
  <remote  name="gitlab"
           fetch="https://gitlab.com/" />

  <!-- DU remote -->
  <remote  name="sucio"
           revision="q10x"
           fetch="https://github.com/UnicornioSucio/" />

  <!-- DU STAGING REPOS -->

  <!-- General -->
  <project path="build/make" name="android_build" remote="sucio" >
    <copyfile src="core/root.mk" dest="Makefile" />
    <linkfile src="CleanSpec.mk" dest="build/CleanSpec.mk" />
    <linkfile src="buildspec.mk.default" dest="build/buildspec.mk.default" />
    <linkfile src="core" dest="build/core" />
    <linkfile src="envsetup.sh" dest="build/envsetup.sh" />
    <linkfile src="target" dest="build/target" />
    <linkfile src="tools" dest="build/tools" />
  </project>
  <project path="build/soong" name="android_build_soong" remote="sucio" >
    <linkfile src="root.bp" dest="Android.bp" />
    <linkfile src="bootstrap.bash" dest="bootstrap.bash" />
  </project>
  <project path="manifest" name="android_manifest" remote="sucio" />

  <!-- Packages -->
  <project path="packages/apps/Bluetooth" name="android_packages_apps_Bluetooth" remote="sucio" />

  <!-- Vendor -->
  <project path="vendor/du" name="android_vendor_du" remote="sucio" >
    <linkfile src="scripts/merge-aosp.sh" dest="merge-aosp.sh" />
  </project>
  <project path="vendor/extras" name="android_vendor_extras" remote="sucio" />
  <project path="vendor/themes" name="android_vendor_themes" remote="sucio" />

</manifest>
